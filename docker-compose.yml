version: "3.9"

services:
  frontend:
    image: ghcr.io/enduser101/mydea-frontend:latest
    ports:
      - "${FRONTEND_PORT:-13245}:3000"
    depends_on:
      - backend
    # build: ./frontend  # uncomment to build locally instead of pulling GHCR

  backend:
    image: ghcr.io/enduser101/mydea-backend:latest
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db/${POSTGRES_DB}
    ports:
      - "${BACKEND_PORT:-8765}:8000"
    depends_on:
      - db
    # build: ./backend  # uncomment to build locally instead of pulling GHCR

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mydea}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mydea}
      POSTGRES_DB: ${POSTGRES_DB:-mydea}
    ports:
      - "${POSTGRES_PORT:-54333}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
